# Devlog 2026-03-24

## 落地页迭代

- **Hero 布局重构**：从居中单栏改为左右两栏（文字+下载按钮在左，截图在右），响应式 ≤900px 折叠为单列
- **去掉 Windows 窗口边框**：截图直接展示，只保留圆角 + 阴影
- **截图列比例调整**：`1fr 1fr` → `2fr 3fr`，gap 从 56px 收窄至 32px，max-width 扩至 1200px
- **Emoji 图标替换为 Lucide SVG**：6 个功能卡片图标全部换成内联 SVG（zap / hard-drive / grid-2x2 / lightbulb / shield / sparkles），颜色跟随 `--indigo` 变量
- **「使用案例」节上线**：新增 3 张真实截图卡片，场景覆盖「搜索生成 PDF 报告」「Excel 数据汇总」「Word 季度总结」，含场景标签 + 描述 + 用户原话引用
- **文案更新**：步骤 2 说明改为「填入你的大模型 API Key，多个主流大模型供应商可选」

## 内容规划

- 创建 `docs/content-plan.md`：完整的使用教程 + 案例建设两阶段计划
  - 阶段一：落地页案例节（✅ 已完成）
  - 阶段二：独立教程页 `/docs`（待建设）

## 图标探索（未完成）

- 探索了两个方向：**USB + 闪光**（方向A）和**机器人 + 公文包**（方向C）
- 尝试 Gemini API 生成：Imagen 4 需要付费 API，AI Studio 免费配额不含图像生成
- 尝试 `baoyu-danger-gemini-web` skill：Cookie 读取失败，未能自动生成
- 用户手动在 Gemini Web 生成方向C图标：造型可以，但配色太粉蜡，与网站不搭
- 转向**龙虾 + 办公**方向（暖色调）：
  - 方向①：龙虾 + 漂浮文档图标
  - 方向③：极简龙虾剪影 + 金色星花
- 两个方向生成结果均不满意，图标设计暂缓，明日继续

## 待办

- [ ] 图标重新设计（暖色调，龙虾元素，方向①③继续探索或换工具）
- [ ] 落地页 Hero 截图显示问题排查（之前截图出现空白区域，原因未查明）
- [ ] 教程页建设（`/docs/quickstart.html` + 5 篇场景教程）

---

## 朋友圈卡片技能上线（Session 续）

### moments-card skill 完整实现

- **6 个 HTML 模板**：`long_template.html`、`infograph_template.html`、`poster_template.html`、`sketchnote_template.html`、`whiteboard_template.html`、`comic_template.html`
  - 所有模板使用 `fonts.loli.net` 替换 Google Fonts（国内可访问），并补充 `PingFang SC` / `Microsoft YaHei` 作为中文回退
  - 长图模板用纯 CSS `div` 圆形替代图片头像，无本地文件依赖
- **7 个参考文件**：`references/taste.md` + 6 个 mode 文件，从零编写（原版未公开）
- **capture.js**：Playwright 截图脚本，优先尝试系统浏览器（`msedge` → `chrome`），避免下载 ~150MB Chromium；仅在均不可用时才使用 playwright 内置 Chromium
- **`package.json`**：仅声明 `playwright` 依赖（~5MB），不预装浏览器
- **`skills.config.json`**：注册 `moments-card`，`order: 215`

### 构建流程更新（`scripts/build-portable-win.js`）

- Step 1d 从「npm install + playwright install chromium」简化为「仅 npm install」
- 理由：Windows 10/11 已内置 Edge，无需额外打包浏览器，节省约 150MB 包体积

### 首页快捷按钮新增

- `public/quick-actions.json`：新增 `moments-card`（DevicePhoneMobileIcon，青色）和 `pptx`（PresentationChartBarIcon，琥珀色）两个按钮，各含 4 个子场景
- `public/quick-actions-i18n.json`：补充 zh/en 文案
  - **关键 Bug 修复**：prompt 字符串中含 `署名"我"` 导致 JSON.parse 全部失败，所有按钮退化为显示 ID（如 "cn-docx"）。将直引号改为中文角引号 `「我」` 修复
- `SKILLs/pptx/SKILL.md`：将 `name: pptx` 改为 `name: PPT 制作`，让 ActiveSkillBadge 显示中文

### 版本归档

- 新建 `CHANGELOG.md`，记录 v2026.3.24 和 v2026.3.22 两个版本

---

## 官网 udiskai.top 更新

- **截图更新**：替换为展示 5 个快捷按钮（生成Word文档、数据分析、教育学习、发朋友圈、PPT制作）的最新 UI 截图
- **新增「内置技能」区块**：8 张技能卡片（Word文档生成、Excel数据分析、PPT制作、PDF处理、网页搜索、邮件收发、朋友圈卡片、Word编辑），含 SVG 图标和描述
- **新增「更新日志」区块**：v2026.3.24（最新 badge）和 v2026.3.22 两个版本条目
- **导航栏**：新增「技能」和「更新」锚点链接
- **下载链接更新**：`123865` → `123684`，导航按钮和页脚链接同步替换

---

## GitHub README 更新

- 删除「Anthropic Claude（推荐，Cowork 模式必选）」标注——中国用户无法使用，不应推荐
- 删除「Cowork 智能体模式需要 Anthropic Claude API Key」说明——实际上其他模型也可完成文件操作
- Anthropic Claude 改为普通列表项，排在国内服务商之后
- 在 logo 下方添加应用截图（`scripts/web-assets/screenshot.png`，宽 800px 居中）

---

## 朋友圈推广图制作

使用 ljg-card skill（`-l` 长图模具）制作首张朋友圈宣传图：

- 技术/工程色调：背景 `#F5F7FA`，强调色 `#3D5A80`
- 开头：「UdiskAI 是一个便携版 AI 办公助手」+ 首字下沉
- 核心能力以 item 条目列出（含新增的朋友圈卡片）
- 金句：「AI 驱动，本地存储，随插随用。」
- 底部：程序图标 + UdiskAI + UdiskAI.top
- 输出：`~/Downloads/UdiskAI更新.png`
